var {{name}} = Marionette.Module.extend({
  startWithParent: false,

  initialize: function(options, Module, App) {
  	{{module}}
  },

  onStart: function(options) {
  },

  onStop: function(options) {
  },
});
