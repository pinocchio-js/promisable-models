var {{globalNamespace}} = {{globalNamespace}} || {};

{{globalNamespace}}.{{namespace}} = function(module, app, Backbone, Marionette, $, _) {
  this.startWithParent = true;

  var Module = {};

  {{> 'mixin.js'}}

  {{> 'model.js'}}

  {{> 'collection.js'}}

  module['{{namespace}}'] = Module;
};
