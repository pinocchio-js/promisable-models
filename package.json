{
  "name": "promisable-models",
  "version": "1.0.0",
  "description": "Backbone models and collections that behaves like promises for Marionette",
  "main": "index.js",
  "scripts": {
    "clean"   : "rm -f lib/promisable.model.js",
    "install" : "npm install && npm run bower -- install",
    "build"   : "npm run clean && node scripts/module-builder >> lib/promisable.model.js",
    "test"    : "npm run clean && npm run build && npm run karma -- start",
    "karma"   : "./node_modules/karma/bin/karma",
    "bower"   : "bower"
  },
  "author": "Jean Carlos Meninno",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.3.12",
    "handlebars": "^2.0.0",
    "js-beautify": "^1.5.4",
    "karma": "^0.12.31",
    "karma-chrome-launcher": "^0.1.7",
    "karma-mocha": "^0.1.10",
    "karma-mocha-reporter": "^0.3.1",
    "karma-phantomjs-launcher": "^0.1.4",
    "mocha": "^2.1.0",
    "when": "^3.7.2"
  }
}
