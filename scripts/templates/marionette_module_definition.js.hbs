var {{namespace}} = {{namespace}} || {};

{{namespace}}.{{module}} = Marionette.Module.extend({
  startWithParent: true,

  initialize: function() {
    this.{{module}} = {};
    var Module = this.{{module}};

    {{{mixin}}}
    {{{model}}}
    {{{collection}}}
  }
});
